services:
  rstudio:
    image: ruk2712/dsci-310-group-17:latest
    ports:
      - "8787:8787"
    environment:
      - DISABLE_AUTH=true
      - WORKING_DIR=/home/rstudio/project
    volumes:
      - .:/home/rstudio/project
    working_dir: /home/rstudio/project
